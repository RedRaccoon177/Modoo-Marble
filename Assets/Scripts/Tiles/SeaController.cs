using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SeaController : Tile
{

    // Start is called before the first frame update
    void Start()
    {
        //transform.position = tilePos;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
